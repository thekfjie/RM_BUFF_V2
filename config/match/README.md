# Match Presets

比赛用配置。

推荐：

1. 从 `../lab/` 复制一份最接近的基础预设
2. 重命名成带比赛信息的文件
3. 只改这份副本，不改 `lab/` 里的基线

建议命名：

- `standalone_yolo_small_blue_match.yaml`
- `standalone_yolo_small_blue_rmuc_day1.yaml`
- `buff_node_red_match.yaml`
