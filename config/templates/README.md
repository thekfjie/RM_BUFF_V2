# Templates

这里放模板，不直接作为最终运行配置。

用途：

- 新建离线配置时，从 `standalone_run_template.yaml` 复制
- 新建 ROS2 参数时，从 `buff_node_template.yaml` 复制
- 新建原始 parameter 文件时，从 `default_params_template.yaml` 复制

模板的目标是“字段齐全、注释清楚”，不是“已经适配某个场景”。
